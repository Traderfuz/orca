name: BATS Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  bats:
    name: BATS Test Suite
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install BATS
        run: |
          git clone https://github.com/bats-core/bats-core.git /tmp/bats-core
          sudo /tmp/bats-core/install.sh /usr/local
          bats --version

      - name: Install BATS helpers
        run: |
          mkdir -p tests/bats/helpers
          git clone https://github.com/bats-core/bats-support.git tests/bats/helpers/bats-support
          git clone https://github.com/bats-core/bats-assert.git tests/bats/helpers/bats-assert

      - name: Run BATS tests
        run: |
          if [ -d "tests/bats" ] && ls tests/bats/*.bats 2>/dev/null | head -1 > /dev/null; then
            bats --tap tests/bats/*.bats
          else
            echo "No BATS test files found in tests/bats/"
            exit 0
          fi

      - name: Run subdirectory BATS tests
        run: |
          if ls tests/bats/**/*.bats 2>/dev/null | head -1 > /dev/null; then
            bats --tap --recursive tests/bats/
          fi
